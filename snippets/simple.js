// if you don't specify a html file, the sniper will generate a div
var app = require("biojs-vis-pinpad");
var instance = new app({
    el: yourDiv
    , width: '250px'
    , height: '300px'
    , highlightColor: 'green'
    , toPin: data
    , text: 'biojs'
});
instance.addElement(site580);
instance.addElement(site342);
instance.addElement(site425);
instance.addElement(catPTM);
